import { TableHeaderProps } from "../types";
/**
 * TableHeader is used by the Table component to render the `<thead>` table content
 *
 * @example
 * <TableHeader
 *  instance={instance}
 *  translations={translations}
 * />
 */
export declare const TableHeader: <T extends Record<string, any>>({ handleRowToggled, instance, selectableRows, translations, }: TableHeaderProps<T>) => JSX.Element;
