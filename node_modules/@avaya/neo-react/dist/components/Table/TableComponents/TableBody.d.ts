import { TableBodyProps } from "../types";
/**
 * TableBody is used by the Table component to render the table body (<tr>s and <td>s)
 *
 * @example
 * <TableBody
 *  handleRowToggled={(selectedRowIds, toggledRow) => { ... }}
 *  instance={instance}
 *  selectableRows={selectableRows}
 * />
 */
export declare const TableBody: <T extends Record<string, any>>({ handleRowToggled, instance, selectableRows, translations, }: TableBodyProps<T>) => JSX.Element;
