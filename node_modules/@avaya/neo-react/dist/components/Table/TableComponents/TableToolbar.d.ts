import { TableToolbarProps } from "../types";
/**
 * TableToolbar is used by the Table component to render the search and action inputs for the table
 *
 * @example
 * <TableToolbar
 *  handleRefresh={handleRefresh}
 *  instance={instance}
 *  readonly={true}
 *  translations={translations}
 * />
 */
export declare const TableToolbar: <T extends Record<string, any>>({ customActionsNode, handleCreate, handleDelete, handleEdit, handleRefresh, instance, readonly, translations, }: TableToolbarProps<T>) => JSX.Element;
