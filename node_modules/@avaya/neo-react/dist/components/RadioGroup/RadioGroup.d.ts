/// <reference types="react" />
import { RadioProps } from "./Radio";
export interface RadioGroupProps {
    radios: RadioProps[];
    groupName: string;
    checked: string;
    inline?: boolean;
    helperText?: string;
    error?: boolean;
    required?: boolean;
    onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
}
export declare const RadioGroup: ({ radios, groupName, checked, inline, helperText, error, required, onChange, }: RadioGroupProps) => JSX.Element;
