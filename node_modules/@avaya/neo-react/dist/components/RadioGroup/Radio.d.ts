/// <reference types="react" />
export interface RadioProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, "id" | "type"> {
    describedBy?: string;
    selected?: string;
    tooltip?: string;
    position?: string;
    onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
    label: string;
    value: string;
}
export declare const Radio: ({ describedBy, selected, tooltip, position, label, checked, value, name, onChange, ...rest }: RadioProps) => JSX.Element;
