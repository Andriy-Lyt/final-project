/// <reference types="react" />
import { CheckboxProps } from "components/Checkbox";
export interface CheckboxGroupProps {
    checkboxes: CheckboxProps[];
    groupName: string;
    defaultChecked?: string[];
    inline?: boolean;
    helperText?: string;
    error?: boolean;
    required?: boolean;
    onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
}
/**
 * Checkbox group is used to render a group of related Checkbox Components.
 *
 * @example
 * <CheckboxGroup
 *  checkboxes=[
      label: "Check 1",
      value: "Check 1",
      checked: true,
      onChange: () => {},
    },
    {
      label: "Check 2",
      value: "Check 2",
      onChange: () => {},
    },
    ]
    groupName="Default Checkbox Group"
    defaultChecked=["Check 1", "Check 4", "Check 6"]
    onChange=() => {},
    />
 *
 */
export declare const CheckboxGroup: ({ checkboxes, groupName, defaultChecked, inline, helperText, error, required, onChange, }: CheckboxGroupProps) => JSX.Element;
