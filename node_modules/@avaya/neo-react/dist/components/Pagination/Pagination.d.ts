import { PaginationProps } from "./PaginationTypes";
/**
 * This component is used to render pagination.
 * It can be used as a standalone component.
 * Is also used by the Table component.
 * It is up to the implementer to provide the correct data,
 * Eg, `currentPageIndex`, `itemsPerPage`, ect.
 *
 * @example
 * <Pagination
    currentPageIndex={pageIndex}
    itemCount={itemArray.length}
    itemsPerPage={itemsPerPage}
    itemsPerPageOptions={[1, 5, 10]}
    onPageChange={(e, newIndex) => {
      e?.preventDefault();
      setPageIndex(newIndex);
    }}
    onItemsPerPageChange={(e, newItemsPerPage) => {
      e?.preventDefault();
      setItemsPerPage(newItemsPerPage);
    }}
 * />
 *
 * @see https://design.avayacloud.com/components/web/tables-web
 */
export declare const Pagination: ({ id, currentPageIndex, itemCount, itemsPerPage, itemsPerPageLabel, itemsPerPageOptions, alwaysShowPagination, itemDisplayType, onPageChange, onItemsPerPageChange, backIconButtonText, nextIconButtonText, tooltipForCurrentPage, tooltipForShownPagesSelect, centerNode, leftNode, rightNode, }: PaginationProps) => JSX.Element;
