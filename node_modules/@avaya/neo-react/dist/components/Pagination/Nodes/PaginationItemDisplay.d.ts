import { PaginationProps } from "..";
/**
 * Displays the currently selected item(s) out of the total number of items.
 * Ie, "1 / 10" for `itemDisplayType="page"`
 * or "1 - 10 / 100" for `itemDisplayType="count"`
 *
 * @example
 * <PaginationItemDisplay
 *  ariaLabelForCurrentPage={"Page Count"}
 *  currentPageIndex={3}
 *  itemCount={50}
 *  itemDisplayType="count"
 *  itemsPerPage={5}
 *  totalPages={10}
 * />
 */
export declare const PaginationItemDisplay: ({ currentPageIndex, itemCount, itemDisplayType, itemsPerPage, tooltipForCurrentPage, totalPages, }: {
    totalPages: number;
} & Pick<PaginationProps, "currentPageIndex" | "itemCount" | "itemDisplayType" | "itemsPerPage" | "tooltipForCurrentPage">) => JSX.Element;
