/// <reference types="react" />
import { TooltipPosition } from "components/Tooltip";
export interface CheckboxProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, "type" | "checked"> {
    checked?: boolean | "indeterminate";
    describedBy?: string;
    isLabelHidden?: boolean;
    label: string;
    onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
    tooltip?: {
        label: string;
        position?: TooltipPosition;
    };
    value: string;
}
/**
 * Checkboxes are used when several choices are available and multiple selections are allowed.
 *
 * @example
 * <Checkbox label="Checkbox 1" value="Checkbox 1" name="Checkbox Group Name"/>
 *
 * @see https://design.avayacloud.com/components/web/checkbox-web
 */
export declare const Checkbox: ({ checked, describedBy, id, isLabelHidden, label, name, onChange, tooltip, value, ...rest }: CheckboxProps) => JSX.Element;
