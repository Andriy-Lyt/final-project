import { FunctionComponent, ReactElement } from "react";
import { AvatarProps } from "components/Avatar";
import { IconProps } from "components/Icon";
import { TooltipPosition } from "components/Tooltip";
export interface ListSectionProps {
    className?: string;
    hover?: boolean;
    avatar?: ReactElement<AvatarProps>;
    actions?: ReactElement<any>[];
    icon?: ReactElement<IconProps>;
    tooltip?: string;
    tooltipPosition?: TooltipPosition;
}
/**
 * This component will typically be used as a child of the List component.
 */
export declare const ListSection: FunctionComponent<ListSectionProps>;
